# Inventory Client

This directory contains the client code for the application.

## Setup

1. Copy environment variables:
   ```bash
   cp .env.local.example .env.local
  ```
2. Update `.env.local` with real values.

